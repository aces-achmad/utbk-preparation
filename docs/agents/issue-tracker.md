# Issue Tracker

This repository currently uses local markdown documents for planning and review.

Current posture:

- no external issue tracker is assumed;
- work may be tracked in repository documentation while the product is still under review;
- if formal issue management is needed later, this file should be updated before using issue-oriented skills.

Default future assumption:

- if no external tracker is configured, create local planning artifacts inside the repository;
- do not assume GitHub Issues, GitLab Issues, Jira, or Linear without an explicit decision.
