[WARNING] [apps/backend/src/app/create-app.ts](/home/DATA/Proyek/AHE/utbk-preparation/apps/backend/src/app/create-app.ts:126) — Error validasi `Zod` akan jatuh ke `500` karena `onError()` hanya memetakan pesan string generik, jadi request body/query yang salah bentuk tampak seperti server bug — Deteksi `ZodError` secara eksplisit dan kembalikan `400` dengan payload error field-level yang stabil.

[WARNING] [apps/backend/src/modules/attempts/services/start-or-resume-attempt.ts](/home/DATA/Proyek/AHE/utbk-preparation/apps/backend/src/modules/attempts/services/start-or-resume-attempt.ts:79) — Payload attempt aktif masih mengirim `options.is_correct` dan `explanationText` ke frontend sebelum submit, sehingga kunci jawaban dan pembahasan bisa dibaca dari network/state tanpa menyelesaikan latihan — Pisahkan DTO practice aktif dari DTO review; untuk attempt aktif kirim hanya data yang perlu dikerjakan, lalu buka `is_correct` dan pembahasan hanya lewat endpoint hasil setelah submit.

[WARNING] [apps/backend/src/modules/attempts/services/autosave-attempt-answer.ts](/home/DATA/Proyek/AHE/utbk-preparation/apps/backend/src/modules/attempts/services/autosave-attempt-answer.ts:20) — Cek status attempt dan `upsertAnswer()` tidak atomik, jadi autosave yang lolos cek saat status masih `active` masih bisa menulis setelah submit lain sudah memfinalisasi attempt — Bungkus autosave/submit dalam transaksi atau ubah write path supaya `UPDATE/INSERT` hanya berhasil bila row attempt masih `active` pada saat write.

[SUGGESTION] [apps/frontend/src/services/api.ts](/home/DATA/Proyek/AHE/utbk-preparation/apps/frontend/src/services/api.ts:18) — `apiFetch()` selalu memaksa `response.json()`, sehingga error HTML/non-JSON dari proxy atau backend akan meledak sebagai `SyntaxError` dan menutupi pesan operasional yang sebenarnya — Tambahkan fallback parsing berbasis `content-type`/`response.text()` lalu normalisasikan pesan error jika respons bukan JSON valid.
